Can you explain this section to me in more detail?  中文回答 const handleImageUpload = (event: React.ChangeEvent) => {     const file = event.target.files?.[0];     if (file) {       setSelectedImage(file);       setImagePreview(URL.createObjectURL(file));     }   };  ```

const toggleDeepSearch = () => { setIsDeepSearch((prev) => (prev === true ? null : true)); };

這是在判斷什麼？

為什麼true 是 false ?? 



